import { PositionImageDirective } from './position-image.directive';

describe('PositionImageDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionImageDirective();
    expect(directive).toBeTruthy();
  });
});
